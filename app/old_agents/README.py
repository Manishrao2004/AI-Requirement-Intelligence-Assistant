# Archive of the original 10 sequential agents (pre-refactor).
# These files are kept for reference only and are NOT imported anywhere.
# The active pipeline uses:
#   app/agents/analysis_agent.py  — Agent 1 (ReAct, validate_requirement tool)
#   app/agents/synthesis_agent.py — Agent 2 (ReAct, check_conflict + find_coverage_gaps tools)
